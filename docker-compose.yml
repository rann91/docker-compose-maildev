version: '3'

services:
  maildev:
    container_name: maildev
    image: djfarrelly/maildev
    restart: always
    ports:
      - '1025:25'
    environment:
      - VIRTUAL_PORT=1080
      - VIRTUAL_HOST
networks:
  default:
    external:
      name: nginx-proxy
